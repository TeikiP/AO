package model;

/**
 * This class handles the colision between Element type objects.
 * It contains only one method which cheks is a colision occurs or not.
 */
public class Colision {


    /**
     * Checks if two Element type objects are coliding.
     * @param e1 The first element to collide
     * @param e2 The second element to collide.
     * @return 0 if there is no colision, 1 if a colision occurs.
     */
    public static int areColiding(Element e1, Element e2){

        return 0;
    }

}
